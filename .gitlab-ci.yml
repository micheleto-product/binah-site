pages:
  stage: deploy
  script:
    - mkdir public
    - cp index.html public/
    - cp -r css public/ || true
    - cp -r js public/ || true
    - cp -r assets public/ || true
  artifacts:
    paths:
      - public
  only:
    - main